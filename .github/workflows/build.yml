name: Build and Release
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    permissions: 
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24 
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Create Env File
        shell: bash # ВАЖНО для правильной кодировки на Windows
        run: echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > .env

      # Шаг с Prisma удален! Drizzle этого не требует.

      - name: Build App
        run: npm run build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish Release
        run: npx electron-builder --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}